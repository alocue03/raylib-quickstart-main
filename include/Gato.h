#pragma once
#include <iostream>
#include <lua.hpp>
class Gato
{
//public:
	//size_t reqCallbackTest(void* content, size_t size, size_t nmemb, void* userp);
	//size_t reqCallback(void* content, size_t size, size_t nmemb, void* userp);
	//int webreq(lua_State* L);
	//int print(lua_State* L);
	//int sum(lua_State* L);

	//void GatoMain();

};

